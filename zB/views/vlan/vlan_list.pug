extends ../layout

block content
  .list-container
    .list-header
      h1.page-title VLAN Verwaltung
      .actions
        a.btn.btn-icon-only(href='/secured/vlan/add', title='Neues VLAN')
          span.btn-icon +
    
    .table-wrapper
      table.vlan-list
        thead
          tr
            th ID
            th Name
            th IP
            th Raum
            th Aktionen
        tbody
          each vlan in vlans
            tr(data-vlan-id=vlan.id)
              td
                span.vlan-id= vlan.id
              td= vlan.name
              td= vlan.ip
              td= vlan.room_name
              td.action-buttons
                a.btn.btn-small.btn-icon-only(href=`/secured/vlan/edit/${vlan.id}`, title='Bearbeiten')
                  span.btn-icon ✎
                button.btn.btn-small.btn-danger.btn-icon-only(type='button', data-delete-vlan=vlan.id, data-vlan-name=vlan.name, title='Löschen')
                  span.btn-icon ✕

block scripts
  script.
    // CSRF-Token für Auto-Initialization bereitstellen
    const csrfToken = '#{csrfToken}';
    // DeleteHandler initialisiert automatisch alle [data-delete-vlan] Buttons
